[PLANS]
- 2026-03-28T00:34:00+08:00 [USER] Set the hero-background defaults to `#0d3b8b` and `64`, hide the Leva controls again, and reduce the shared header height by 20px.
- 2026-03-28T00:24:00+08:00 [USER] Change the configurable hero background from a top-down fade to a radial gradient centered behind the globe.
- 2026-03-28T00:13:06.9870180+08:00 [USER] Remove the remaining Three.js `Clock` deprecation warning from the hero `ParticleEarth` embed and make the restored Leva panel fully readable again.
- 2026-03-27T15:59:29.2068739Z [USER] Restore Leva controls on the hero `ParticleEarth` embed and add a hero-background gradient whose color and length can be adjusted from Leva.
- 2026-03-27T15:43:37.1886598Z [USER] Implement a mobile-friendly frontend pass across `/`, `/contact`, `/rnd`, `/rnd/:slug`, and `/project/:slug`: unify breakpoints/tokens, keep the drawer nav, simplify the mobile hero, make standalone routes header-aware, and ship a reduced-interactive `ParticleEarth` mobile embed.
- 2026-03-27T09:02:55Z [USER] Compare the live localhost site against the CVIO `index_onepage` reference with Playwright, then do another frontend pass focused on navigation structure, hero placement, typography scale, and page padding.
- 2026-03-27T08:10:03Z [USER] Implement a dark-only, CVIO-inspired frontend revamp: make `/` the canonical one-page experience, keep Sanity portfolio/blog data and EmailJS contact handling intact, retain `/project/:slug`, `/rnd`, `/rnd/:slug`, and `/contact`, remove the unwanted CVIO sections, and embed `ParticleEarth` as the first-screen hero.
- 2026-02-10T08:31:14Z [TOOL] [MILESTONE] Prior work before this redesign focused on the old Home hero ripple/fluid behavior, theme transitions, and project-card title formatting; treat that as historical context only.

[DECISIONS]
- 2026-03-28T00:34:00+08:00 [USER] The hero should keep the gradient controls in code but hide Leva for now, so the current embed uses fixed defaults without exposing the panel.
- 2026-03-28T00:13:06.9870180+08:00 [CODE] The hero should no longer darken the entire `ParticleEarth` iframe, because iframe-wide filtering also dims Leva; the atmospheric read should come from the existing hero shade plus in-iframe gradients instead.
- 2026-03-27T15:59:29.2068739Z [USER] The hero `ParticleEarth` iframe should expose Leva controls again in embed mode, and the background gradient should be adjustable from those controls rather than remaining hardcoded in CSS.
- 2026-03-27T15:43:37.1886598Z [USER] The mobile pass should keep the existing drawer-style navigation instead of introducing a second mobile nav pattern, and `ParticleEarth` should stay interactive on phones via a reduced embed mode instead of falling back to static media.
- 2026-03-27T15:43:37.1886598Z [CODE] The shared responsive breakpoints are now `phone <= 640`, `tablet <= 1023`, `desktop >= 1024`, and `wide rails >= 1180`, with layout tokens centralized as `--site-gutter`, `--section-gap`, and `--panel-padding`.
- 2026-03-27T15:20:00Z [USER] The shared site accent should now be `#90D5FF`, superseding the earlier `#8CB6DE` choice; all derived accent surfaces, glows, and hover/focus states should move with it.
- 2026-03-27T15:15:00Z [USER] The About section should no longer include the three-card workflow strip (`Discover`, `Prototype`, `Polish`); it should flow directly from the intro/stats block into the capabilities grid.
- 2026-03-27T15:12:00Z [USER] The shared site accent should shift from the previous blue to `#8CB6DE`, and all derived accent surfaces/glows should update with it so buttons, chips, nav indicators, and hero highlights stay consistent.
- 2026-03-27T15:08:00Z [CODE] The desktop header nav should reserve the same right-side inset as the fixed `Follow Me` rail, so the last nav item (`Contact`) aligns with the rail instead of extending farther right.
- 2026-03-27T14:59:52.5301322Z [CODE] The shared header should no longer surface `Download CV` in either the desktop brand cluster or the mobile drawer; the fixed header is now navigation-only.
- 2026-03-27T14:58:38.3775074Z [CODE] The simplified `/rnd` page still needs route-level top padding that clears the fixed shared header; otherwise the `Blog` title renders under the header even though it exists in the DOM.
- 2026-03-27T14:53:57.3665745Z [CODE] The dedicated `/rnd` page should no longer use its older boxed hero/introduction treatment; it should follow the simplified title-only pattern with a single `Blog` heading above the post grid.
- 2026-03-27T14:50:39.1459363Z [CODE] The simplified homepage section headers should align to the first line of inner panel content, not the outer card border, so titles like `About`, `Work`, and `Contact` read on the same text column as the content beneath them.
- 2026-03-27T14:36:55.0034935Z [CODE] The main homepage should no longer render a blog preview section; the fixed `Blog` nav item should route to the dedicated `/rnd` journal page instead of anchoring into `/`.
- 2026-03-27T14:31:15.9140421Z [CODE] The homepage navigation should now track the visible anchored section during scroll, not just the current hash from clicks, so the fixed desktop nav stays informative on long one-page scrolls.
- 2026-03-27T14:31:15.9140421Z [CODE] The fixed left email rail and right follow rail should fade out as the footer enters the viewport, preventing the desktop rails from visually colliding with the footer band.
- 2026-03-27T14:31:15.9140421Z [CODE] The blog preview should keep its CTA on the shared blue accent system and constrain itself when there are fewer than three posts, so wide screens do not leave a single card floating in excess whitespace.
- 2026-03-27T14:31:15.9140421Z [CODE] The contact section should defensively handle missing `CONTACT_CONTENT.contactMethods` data instead of assuming it exists, preventing a runtime crash during local dev/HMR edge cases.
- 2026-03-27T14:16:44Z [CODE] The shared footer should now be minimal-only: remove the large footer panel with intro/navigation/connect columns and leave only the bottom copyright line.
- 2026-03-27T14:15:11Z [CODE] The footer should no longer show the implementation note about React, Sanity, and ParticleEarth; keep only the copyright line in the footer bottom row.
- 2026-03-27T14:13:38Z [CODE] The homepage sections should have more vertical separation from each other, and the simplified section headers (`About`, `Work`, `Blog`, `Contact`) should use a smaller title scale than the previous oversized clamp.
- 2026-03-27T14:11:06Z [CODE] The contact-method cards should now run horizontally across the width on desktop instead of stacking vertically, while still collapsing back to one column on smaller screens.
- 2026-03-27T14:09:32Z [CODE] The contact methods panel should now sit below the message form on desktop as well; the contact section should use a single-column stacked layout instead of the previous two-column split.
- 2026-03-27T14:06:49Z [CODE] The Works section should now be a single unified project list: remove the separate featured block, place featured items first inside the main filtered grid, and drop the `Project Archive` label.
- 2026-03-27T14:05:11Z [CODE] The right-hand contact panel should now be method-only; remove the collaboration intro copy block and keep just the direct contact method cards.
- 2026-03-27T14:03:50Z [CODE] The Contact section should now follow the same title-only pattern as About, Work, and Blog: a single `Contact` heading with the boxed intro copy removed, while preserving the form and contact-info panels below.
- 2026-03-27T14:02:10Z [CODE] The Blog section should now follow the same stripped-down heading pattern as About and Work: a single `Blog` title with the left-side eyebrow/subtitle copy removed, while keeping the `View All Posts` action.
- 2026-03-27T14:00:45Z [CODE] The Works section should mirror the About section more closely by using a single clean section title (`Work`) above the content, without restoring the older eyebrow/subtitle intro block.
- 2026-03-27T13:57:00Z [CODE] The Works section should no longer show its top-level intro copy block; keep the `#works` anchor and portfolio content, but start the section directly with the featured/archive panels.
- 2026-03-27T13:46:22Z [CODE] The second-section heading should be simplified to `About`, and the resume-section action buttons plus capability tool chips should use the shared blue accent treatment instead of the neutral gray button/chip styling.
- 2026-03-27T11:43:39Z [CODE] The post-hero content rhythm should no longer use the earlier narrow 920px container; widen the shared content width to 1120px, remove the resume eyebrow label, and let section titles wrap across a broader measure so the desktop sections feel less squeezed.
- 2026-03-27T11:43:39Z [CODE] The fixed desktop header should sit on a solid background matching the base page color, and the resume stats panel should use solid/transparent surfaces only so tinted gradients do not bleed through the right-hand cards.
- 2026-03-27T11:36:17Z [CODE] The desktop left hero rail should now be contact-only; remove the separate availability/status line so the side framing stays cleaner and less crowded.
- 2026-03-27T10:09:16Z [CODE] The desktop left contact/status rail and right follow rail should stay pinned to the viewport during page scroll, not remain scoped to the initial hero section, so the homepage keeps the reference-style side framing while later sections move underneath.
- 2026-03-27T09:46:00Z [CODE] The site-wide accent was switched from green to blue using `#4988C4` as the primary accent token, with matching derived soft/surface/hover values so the home hero, buttons, nav indicators, and section accents stay visually coherent.
- 2026-03-27T09:44:00Z [CODE] The desktop `Hello, I'm...` line should live in the middle-bottom zone instead of the left rail, and its wording should follow a tighter role-plus-output pattern: one primary role plus a concrete specialization, avoiding stacked discipline lists in the hero.
- 2026-03-27T09:38:00Z [CODE] For the wide-screen hero polish, the header brand mark was removed, the desktop scroll arrow was shifted into the right-side empty field, and the broad availability line was shortened to `Available for freelance, contract, and project work` so it stays professional and fits the left rail.
- 2026-03-27T09:30:00Z [CODE] The desktop home shell should follow the reference structure more literally than the prior mobile-first pass: full-width fixed header with horizontal nav on wide screens, bottom-left contact rail, bottom intro block, bottom-center scroll cue, and right-side follow rail inside the hero stage.
- 2026-03-27T09:30:00Z [CODE] `ParticleEarth` embed assets must resolve relative to `/particle-earth/` and its hidden Leva root must be forcibly suppressed in embed mode; otherwise the iframe fails or shows debug UI inside the hero.
- 2026-03-27T09:02:55Z [CODE] Move the desktop shell closer to the reference by replacing the pill nav with a sparse fixed header: top-left brand plus CV link, top-right journal link plus hamburger, and a drawer for full navigation.
- 2026-03-27T09:02:55Z [CODE] Recompose the hero around a centered display statement and a smaller lower support block; keep `ParticleEarth` in the first screen, but treat it as dark atmospheric motion instead of the dominant readable layer.
- 2026-03-27T09:02:55Z [CODE] Tighten the entire page rhythm toward the reference by reducing `--site-max-width`, increasing desktop side gutters, switching to Roboto/Roboto Mono, and flattening rounded cards/buttons into sharper bordered panels.
- 2026-03-27T08:10:03Z [CODE] `/` is the canonical anchored route with `Home`, `Resume`, `Works`, `Blog`, and `Contact` sections; `/about` redirects to `/#resume` instead of remaining a separate page.
- 2026-03-27T08:10:03Z [CODE] The public frontend now uses one dark-only visual system; retained routes (`/project/:slug`, `/rnd`, `/rnd/:slug`, `/contact`) were restyled to share that shell instead of preserving the previous mixed theme setup.
- 2026-03-27T08:10:03Z [CODE] `ParticleEarth` is integrated as an isolated built iframe at `/particle-earth/index.html?embed=1`; do not merge its separate React/Three runtime into the main React 18 app.
- 2026-03-27T08:10:03Z [CODE] The verified hero-sync workflow is `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\syncParticleEarth.ps1`; do not treat an npm wrapper as canonical in this sandbox because nested npm/bash launches are unreliable here.

[PROGRESS]
- 2026-03-28T00:34:00+08:00 [CODE] Updated `ParticleEarth/src/App.tsx` and `ParticleEarth/src/index.css` so the hidden hero-gradient defaults are now `#0d3b8b` and `64%`, and the embed path no longer renders `<Leva />`.
- 2026-03-28T00:34:00+08:00 [CODE] Updated `ParticleEarth/src/index.css` to force any residual embed Leva root hidden with `display: none !important`, covering the prior `useControls` auto-root behavior.
- 2026-03-28T00:34:00+08:00 [CODE] Updated `frontend/src/components/SiteHeader.jsx` and `frontend/src/App.jsx` so the shared header baseline drops from `122px/96px` to `102px/76px`, with matching root `--site-header-height` defaults.
- 2026-03-28T00:24:00+08:00 [CODE] Updated `ParticleEarth/src/index.css` so the Leva-driven hero background layer now uses `radial-gradient(circle at 50% 42%, var(--hero-gradient-color) 0%, transparent var(--hero-gradient-length))`, aligning the configurable gradient center with the globe's visual center instead of fading top-to-bottom.
- 2026-03-28T00:13:06.9870180+08:00 [CODE] Patched `ParticleEarth/node_modules/@react-three/fiber/dist/events-5a94e5eb.esm.js` plus the matching CJS bundles to replace the deprecated `THREE.Clock` store clock with a local `THREE.Timer`-backed compatibility clock that preserves Fiber's expected `start()`, `stop()`, `getDelta()`, `elapsedTime`, and `oldTime` API.
- 2026-03-28T00:13:06.9870180+08:00 [CODE] Updated `frontend/src/components/ParticleEarthFrame.jsx` to remove the iframe-wide brightness/contrast filter so the Leva panel renders at normal opacity and contrast inside the hero.
- 2026-03-27T15:59:29.2068739Z [CODE] Updated `ParticleEarth/src/App.tsx` to render Leva in embed mode again, split out a `Hero Background` Leva folder, and bind `heroGradientColor` / `heroGradientLength` into CSS variables on the shell so the iframe hero background can be tuned live.
- 2026-03-27T15:59:29.2068739Z [CODE] Updated `ParticleEarth/src/index.css` so the shell background now includes a configurable linear gradient, and added embed-specific Leva positioning rules that place the restored panel below the site header with scrollable height constraints.
- 2026-03-27T15:43:37.1886598Z [CODE] Added `frontend/src/styles/breakpoints.js` and rewired the active frontend to use those breakpoints across the shell, header, hero, About/Work/Contact sections, and standalone route layouts; mobile tokens now reduce gutters/section gaps/panel padding and lower the decorative grid strength below tablet width.
- 2026-03-27T15:43:37.1886598Z [CODE] Reworked `frontend/src/components/home/HomeHero.jsx` into a mobile-safe viewport-capped hero with shared content source fields (`displayLead` / `displayAccent`), two mobile actions only, hidden desktop rails below the wide breakpoint, and phone-safe width clamping.
- 2026-03-27T15:43:37.1886598Z [CODE] Updated `frontend/src/components/ParticleEarthFrame.jsx` to append `mobile=1` for phone-sized embeds and tuned iframe scaling/filtering by breakpoint; updated `ParticleEarth/src/App.tsx` so mobile embed mode lowers DPR and uses lighter particle density/size/opacity defaults while keeping drag/inertia active.
- 2026-03-27T15:43:37.1886598Z [CODE] Updated `frontend/src/components/ContactSection.jsx`, `frontend/src/components/RnDBlog.jsx`, `frontend/src/components/ProjectDetail.jsx`, and `frontend/src/components/BlogPost.jsx` so standalone routes clear the fixed header, mobile widths stop using `100vw`, panels use shared padding, and long-form/project content stays single-column below `1024px`.
- 2026-03-27T15:20:00Z [CODE] Updated `frontend/src/styles/theme.js` again to replace the `#8CB6DE` palette with `#90D5FF` plus matching rgba/light/hover variants, and updated `frontend/src/components/home/HomeHero.jsx` to replace the remaining hardcoded hero wash with the new accent rgba.
- 2026-03-27T15:15:00Z [CODE] Updated `frontend/src/components/home/ResumeSection.jsx` to remove the `WORKFLOW_PHASES` render block and delete the now-unused workflow styles/import so the About section jumps straight from the intro/stat panels to the capability cards.
- 2026-03-27T15:12:00Z [CODE] Updated `frontend/src/styles/theme.js` to replace the old accent palette with `#8CB6DE` plus matching soft/surface/hover/focus variants, and updated `frontend/src/components/home/HomeHero.jsx` to replace the remaining hardcoded hero glow with the new accent rgba.
- 2026-03-27T15:08:00Z [CODE] Updated `frontend/src/components/SiteHeader.jsx` so the desktop nav adds a small right padding (`clamp(0.9rem, 1vw, 1rem)`), shifting the last nav item left to match the right-side rail inset on wide screens.
- 2026-03-27T14:59:52.5301322Z [CODE] Updated `frontend/src/components/SiteHeader.jsx` to remove the top-left `Download CV` link, drop the drawer resume CTA, and clean up the now-unused `EXTERNAL_LINKS`, `BrandBlock`, `BrandMeta`, and `DrawerCta` header code.
- 2026-03-27T14:58:38.3775074Z [CODE] Updated `frontend/src/components/RnDBlog.jsx` so the route-level `Page` padding-top uses `calc(var(--site-header-height, 96px) + clamp(1.4rem, 4vw, 3rem))`, ensuring the simplified `Blog` title clears the fixed header.
- 2026-03-27T14:53:57.3665745Z [CODE] Updated `frontend/src/components/RnDBlog.jsx` to remove the boxed hero copy/meta block, replace it with a simple left-aligned `Blog` section header, and drop the now-unused latest-post summary data/styles.
- 2026-03-27T14:50:39.1459363Z [CODE] Updated `frontend/src/components/home/ResumeSection.jsx`, `frontend/src/components/home/WorksSection.jsx`, and `frontend/src/components/ContactSection.jsx` so their `SectionHeader` blocks add desktop left padding equal to the first panel’s inner padding, while still resetting to `0` on smaller screens.
- 2026-03-27T14:36:55.0034935Z [CODE] Updated `frontend/src/content/siteContent.js` so `HOME_NAV_ITEMS` now declare section vs route behavior and point `Blog` to `/rnd`; updated `frontend/src/components/SiteHeader.jsx` to use those explicit routes, exclude `Blog` from home scroll tracking, and keep `Blog` active on `/rnd` and `/rnd/:slug`; updated `frontend/src/pages/Home.jsx` to remove the Sanity blog-preview query and the `BlogPreviewSection` from the homepage.
- 2026-03-27T14:31:15.9140421Z [CODE] Updated `frontend/src/components/SiteHeader.jsx` to track the visible home section via scroll position and use that state for desktop/drawer active-link styling instead of relying only on `location.hash`.
- 2026-03-27T14:31:15.9140421Z [CODE] Updated `frontend/src/components/home/HomeHero.jsx` to compute footer proximity and apply opacity/translate transitions to the fixed desktop side rails so they fade before the footer.
- 2026-03-27T14:31:15.9140421Z [CODE] Updated `frontend/src/components/ContactSection.jsx` to use safe fallbacks for the contact title/methods and only render the methods panel when `contactMethods` is an array with entries.
- 2026-03-27T14:31:15.9140421Z [CODE] Updated `frontend/src/components/home/BlogPreviewSection.jsx` to slice to three preview posts, constrain one/two-post desktop widths, and restyle `View All Posts` with the shared accent CTA treatment.
- 2026-03-27T14:31:15.9140421Z [CODE] Updated `frontend/src/App.jsx` to widen `--site-max-width` from `1120px` to `1240px`, reducing the detached desktop gutter between the fixed rails and the main content column.
- 2026-03-27T14:16:44Z [CODE] Updated `frontend/src/components/SiteFooter.jsx` to remove the entire `FooterInner` panel plus all related intro/navigation/connect content and unused imports/constants, leaving a stripped footer with only `FooterBottom`.
- 2026-03-27T14:15:11Z [CODE] Updated `frontend/src/components/SiteFooter.jsx` to remove the secondary footer-bottom text (`Built with React, Sanity, and a synced ParticleEarth hero.`), leaving the footer bottom row with only the copyright item.
- 2026-03-27T14:13:38Z [CODE] Updated `frontend/src/pages/Home.jsx`, `frontend/src/components/home/ResumeSection.jsx`, `frontend/src/components/home/WorksSection.jsx`, `frontend/src/components/home/BlogPreviewSection.jsx`, and `frontend/src/components/ContactSection.jsx` to add a larger homepage section gap and reduce the main section-title clamp across the simplified headers.
- 2026-03-27T14:11:06Z [CODE] Updated `frontend/src/components/ContactSection.jsx` so `MethodList` uses a three-column desktop grid for the contact-method cards, falling back to one column below `960px`.
- 2026-03-27T14:09:32Z [CODE] Updated `frontend/src/components/ContactSection.jsx` so `BodyGrid` now uses a single-column layout, placing the contact methods panel beneath the message form for both the homepage and standalone contact route.
- 2026-03-27T14:06:49Z [CODE] Refactored `frontend/src/components/home/WorksSection.jsx` to merge featured and archive items into one ordered/filterable grid, remove the standalone featured/archive block headers, and reuse the archive-card presentation for the combined project list.
- 2026-03-27T14:05:11Z [CODE] Updated `frontend/src/components/ContactSection.jsx` to remove the right-panel collaboration intro copy (`Open for selected collaborations` / title / paragraph), leaving the side panel to start directly with the contact method list.
- 2026-03-27T14:03:50Z [CODE] Updated `frontend/src/content/siteContent.js` and `frontend/src/components/ContactSection.jsx` so the contact section now renders a simple `Contact` heading only, removing the old eyebrow/subtitle/meta header block while leaving the shared form and info panels intact.
- 2026-03-27T14:02:10Z [CODE] Updated `frontend/src/content/siteContent.js` and `frontend/src/components/home/BlogPreviewSection.jsx` so the blog preview section now renders a single `Blog` heading and the existing `View All Posts` button, with the prior eyebrow/subtitle block removed.
- 2026-03-27T14:00:45Z [CODE] Updated `frontend/src/content/siteContent.js` and `frontend/src/components/home/WorksSection.jsx` so the section now displays a simple `Work` title above the featured/archive panels, using the same heading treatment as the About section.
- 2026-03-27T13:57:00Z [CODE] Simplified `frontend/src/components/home/WorksSection.jsx` by removing the top Works header markup plus its unused intro styles/import, leaving the section to open directly on `Featured Work`.
- 2026-03-27T13:46:22Z [CODE] Updated `frontend/src/content/siteContent.js` and `frontend/src/components/home/ResumeSection.jsx` so the resume title now reads `About`, while the demo-resume buttons and capability tool chips use accent-colored borders/backgrounds with blue text and hover treatment.
- 2026-03-27T11:43:39Z [CODE] Updated `frontend/src/App.jsx`, `frontend/src/styles/theme.js`, `frontend/src/components/SiteHeader.jsx`, `frontend/src/components/home/ResumeSection.jsx`, `frontend/src/components/home/WorksSection.jsx`, `frontend/src/components/home/BlogPreviewSection.jsx`, and `frontend/src/components/ContactSection.jsx` to widen content width, solidify shared surfaces, remove the resume eyebrow, widen section-title measures, and make the fixed header opaque.
- 2026-03-27T11:36:17Z [CODE] Simplified `frontend/src/components/home/HomeHero.jsx` by removing the `Status` entry from `HERO_CONTACT_LINES`, leaving only the email line in the desktop left rail.
- 2026-03-27T10:09:16Z [CODE] Updated `frontend/src/components/home/HomeHero.jsx` so `DesktopContact` and `DesktopFollow` use `position: fixed` with a high stacking context, allowing both side rails to remain visible while the one-page sections scroll underneath them on desktop.
- 2026-03-27T09:52:00Z [CODE] Replaced the desktop hero introduction with the user's exact preferred sentence: `I'm Yu Xuan, a 3D motion designer creating cinematic visuals, interactive experiences, and polished post-production.`
- 2026-03-27T09:46:00Z [CODE] Updated the shared frontend accent palette in `frontend/src/styles/theme.js` to blue (`#4988C4`) and replaced the remaining hardcoded green glow in `HomeHero.jsx`, then verified the live localhost hero visually with Playwright.
- 2026-03-27T09:44:00Z [TOOL] Searched the web for portfolio-intro inspiration and then rewrote the desktop hero introduction to `Hello, I'm Yu Xuan, a motion designer focused on 3D visuals and interactive websites.` before centering that block in the desktop hero.
- 2026-03-27T09:38:00Z [CODE] Applied a final hero polish pass requested by the user: removed the top-left green dot from `SiteHeader`, moved the desktop scroll cue rightward to clear the intro text, and updated both hero/contact availability copy to the shorter professional line.
- 2026-03-27T09:30:00Z [TOOL] Used Playwright again at wide-screen size against the reference and localhost, then rebuilt the desktop header/hero so the local site now shows a horizontal desktop nav, full-height one-page hero, contact/follow rails, and no desktop hamburger.
- 2026-03-27T09:30:00Z [CODE] Fixed the `ParticleEarth` embed pathing by setting Vite `base: './'`, switching the elevation texture to `import.meta.env.BASE_URL`, and hiding `#leva__root` in embed mode so the hero globe mounts cleanly inside `/particle-earth/index.html?embed=1`.
- 2026-03-27T09:30:00Z [CODE] Fixed `frontend/scripts/syncParticleEarth.mjs` to resolve paths from the script location instead of the current working directory, so the canonical sync command now copies the built earth bundle into `frontend/public/particle-earth` reliably.
- 2026-03-27T09:02:55Z [TOOL] Used Playwright against both `https://bslthemes.com/html/cvio/index_onepage.html` and `http://localhost:5173/` to compare shell structure, hero composition, measured content width, and section framing before editing.
- 2026-03-27T09:02:55Z [CODE] Reworked `SiteHeader`, `HomeHero`, `ParticleEarthFrame`, and the home section components so the local site now uses the reference-style fixed header + drawer, narrower layout width, smaller section headings, darker hero treatment, and flatter bordered panels.
- 2026-03-27T09:02:55Z [TOOL] Post-pass verification succeeded with `frontend` `npm run lint`, `frontend` `npm run build`, and Playwright checks of the local hero plus drawer menu on `http://localhost:5173/`.
- 2026-03-27T08:10:03Z [CODE] Rebuilt the frontend shell around a single `siteTheme`, replaced the old theme-toggle wiring, added CVIO-style global chrome, and converted home navigation to anchored sections through `App.jsx`, `SiteHeader`, and `SiteFooter`.
- 2026-03-27T08:10:03Z [CODE] Replaced the old home page with modular sections (`HomeHero`, `ResumeSection`, `WorksSection`, `BlogPreviewSection`, `ContactSection`) that reuse existing Sanity project/post queries and current about/contact content.
- 2026-03-27T08:10:03Z [CODE] Restyled `/project/:slug`, `/rnd`, `/rnd/:slug`, and `/contact`; `/contact` now reuses the extracted `ContactSection` so EmailJS behavior is shared between the home section and standalone route.
- 2026-03-27T08:10:03Z [CODE] Added `ParticleEarth` embed mode, synced the earth build into `frontend/public/particle-earth`, and updated `README.md` plus `frontend/README.md` to document the new route model and sync workflow.
- 2026-03-27T08:10:03Z [TOOL] Verification completed successfully with `frontend` `npm run lint`, `frontend` `npm run build`, `ParticleEarth` `npm run lint`, `ParticleEarth` `npm run test:unit`, `ParticleEarth` `npm run build`, `ParticleEarth` `npm run test:e2e`, and `powershell -NoProfile -ExecutionPolicy Bypass -File frontend/scripts/syncParticleEarth.ps1`.

[DISCOVERIES]
- 2026-03-28T00:34:00+08:00 [TOOL] Playwright on `http://localhost:5173/` measured the live shared header at `103px` with `HeaderInner` `min-height: 102px`; the extra pixel comes from the fixed header's 1px bottom border, so the requested 20px reduction is effectively in place.
- 2026-03-28T00:34:00+08:00 [TOOL] Playwright inspection of the hero iframe confirmed `levaVisible: false`, `--hero-gradient-color: #0d3b8b`, and `--hero-gradient-length: 64%` after the sync.
- 2026-03-28T00:24:00+08:00 [TOOL] Playwright inspection of the live iframe confirmed the computed `background-image` now begins with `radial-gradient(circle at 50% 42%, rgba(8, 21, 40, 0.86) 0%, transparent 58%)`, which matches the intended globe-centered hero background.
- 2026-03-28T00:13:06.9870180+08:00 [TOOL] Playwright stack capture showed the warning did not come from scene code or `three-stdlib`; it originated in `@react-three/fiber`'s render-store initialization (`clock: new THREE.Clock()` in `dist/events-5a94e5eb.esm.js`), so the in-repo fix had to sit at the Fiber/Three boundary.
- 2026-03-28T00:13:06.9870180+08:00 [TOOL] The Leva readability issue was caused by `frontend/src/components/ParticleEarthFrame.jsx` applying a brightness/contrast filter to the entire iframe; Playwright later confirmed the corrected runtime state as `iframeFilter: none`, `panelOpacity: 1`, and `panelBackdropFilter: none`.
- 2026-03-27T15:59:29.2068739Z [TOOL] The current Leva build no longer mounts a stable `#leva__root` in this app version; the reliable embed-positioning selector is the direct Leva panel node under `.app-shell` (`.app-shell > [class*='leva-c-']`), which Playwright confirmed after the patch with `top: 108px` and `right: 16px`.
- 2026-03-27T15:43:37.1886598Z [TOOL] Playwright on `http://localhost:5173/` showed the phone hero originally overflowing because the hero/section shells still relied on `100vw` math; replacing those width calculations with container-based widths removed the real `375px`, `390px`, and `1024px` overflow, while the remaining `320px` `scrollWidth` mismatch is a headless client-width artifact with `window.scrollX` still pinned to `0` because `body { overflow-x: hidden; }`.
- 2026-03-27T15:43:37.1886598Z [TOOL] The mobile embed verification succeeded through Playwright by dragging inside the iframe scene and then wheel-scrolling the page; after release, `window.scrollY` advanced from `0` to `900`, which confirms the embed does not trap page scrolling after interaction.
- 2026-03-27T15:20:00Z [TOOL] A follow-up source sweep of `frontend/src` found no remaining `#8CB6DE`, `#A6C9E8`, `rgba(140, 182, 222, ...)`, or `rgba(166, 201, 232, ...)` references after the new palette swap, so the accent is fully centralized on `#90D5FF`.
- 2026-03-27T15:15:00Z [TOOL] The workflow strip was fully local to `ResumeSection.jsx`; removing its JSX, styles, and `WORKFLOW_PHASES` import was sufficient, and a source sweep afterward found none of `WORKFLOW_PHASES`, `Creative workflow`, `Discover`, `Prototype`, or `Polish` remaining in that file.
- 2026-03-27T15:12:00Z [TOOL] A source sweep of `frontend/src` after the patch found no remaining `#4988C4` or `rgba(73, 136, 196, ...)` references, so the new accent is fully centralized on the `#8CB6DE` palette.
- 2026-03-27T15:08:00Z [TOOL] Playwright measurement on `http://localhost:5173/` at `1920x1080` showed the pre-fix desktop nav ending too far right; after adding desktop nav right padding, `Contact` ended at `1868px` and the `Follow Me` label ended at `1869px`, effectively sharing the same right inset.
- 2026-03-27T14:59:52.5301322Z [TOOL] The visible `Download CV` links were both isolated inside `SiteHeader.jsx`; removing them did not affect routing, section tracking, or footer resume links, and lint/build still pass.
- 2026-03-27T14:58:38.3775074Z [TOOL] Playwright confirmed the overlap bug directly: before the patch, the fixed header bottom sat at `123px` while the `/rnd` `Blog` heading started at `48px`; after the padding fix, the heading starts at `171px`, so it is fully visible below the header.
- 2026-03-27T14:53:57.3665745Z [TOOL] Playwright verification on `http://localhost:5173/rnd` now shows only a single `Blog` heading followed directly by the post list; the old boxed introduction/meta block is no longer rendered.
- 2026-03-27T14:50:39.1459363Z [TOOL] Playwright measurement after the alignment patch on `http://localhost:5173/#resume` reported `aboutLeft: 81` vs `aboutTextLeft: 82` and `contactLeft: 78` vs `contactTextLeft: 79`, confirming the section headers now line up with the inner text column rather than the panel border.
- 2026-03-27T14:36:55.0034935Z [TOOL] The dedicated R&D page at `/rnd` already loads the full blog list, so removing the homepage preview only required nav/data cleanup rather than any new CMS query or route work.
- 2026-03-27T14:36:55.0034935Z [TOOL] Playwright verification on `http://localhost:5173/` showed the homepage now jumping from `Work` directly to `Contact`, with the `Blog` header link pointing to `/rnd`; clicking it opened the existing journal page and kept `Blog` visually active in the header.
- 2026-03-27T14:31:15.9140421Z [TOOL] Direct `npm run lint` / `npm run build` still route through Git Bash and fail here with Win32 error 5 signal-pipe/file-mapping crashes; invoking `node .\\node_modules\\eslint\\bin\\eslint.js .` works for lint, and `node .\\node_modules\\vite\\bin\\vite.js build` succeeds when run with escalated permissions because Vite/esbuild otherwise hits `spawn EPERM`.
- 2026-03-27T14:31:15.9140421Z [TOOL] Fresh Playwright verification on `http://localhost:5173/` after the patch showed the desktop nav switching sections as expected (`Resume` active near `1300`, `Works` near `2200`, `Blog` near `3400`, `Contact` near `4050`), the About heading shifted left to `333px` with `--site-max-width: 1240px`, and the blog preview CTA using the blue accent colors.
- 2026-03-27T14:31:15.9140421Z [TOOL] At the footer boundary, Playwright measured both fixed desktop rails at `opacity: 0.110796` while the footer top sat at `1015px`, which confirms the fade logic is preventing the prior hard overlap.
- 2026-03-27T14:31:15.9140421Z [TOOL] The previous `ContactSection` React error no longer appears in the refreshed page state; Playwright reported `contactErrorVisible: false` after the defensive `contactMethods` fallback was added.
- 2026-03-27T14:16:44Z [TOOL] The large footer panel was self-contained inside `SiteFooter.jsx`; removing it also eliminated all `react-router-dom`, nav-link, social-link, and resume-link footer dependencies, and lint/build still pass without replacement layout work.
- 2026-03-27T14:15:11Z [TOOL] The implementation note was isolated to a single `<span>` in `SiteFooter.jsx`; removing it did not require any layout changes, and lint/build still pass.
- 2026-03-27T14:13:38Z [TOOL] Homepage inter-section spacing is centralized in `Home.jsx` via the page grid gap, while the simplified section-header sizing still lives locally in each section component; updating those five locations was enough to increase separation and lower the title scale with lint/build still passing.
- 2026-03-27T14:11:06Z [TOOL] The contact-method orientation is fully controlled by `MethodList`; changing it to a three-column grid is enough to create a horizontal strip on desktop without touching the card component itself, and lint/build still pass.
- 2026-03-27T14:09:32Z [TOOL] The contact section layout is controlled entirely by `BodyGrid`; switching it to a single-column grid is sufficient to move the methods panel below the form without affecting form behavior or the standalone `/contact` route, and lint/build still pass.
- 2026-03-27T14:06:49Z [TOOL] The featured/archive split in `WorksSection.jsx` was purely presentational; collapsing it into a single ordered list only required changing the item ordering and header rendering, and the tag filter works cleanly across the full project set with lint/build still passing.
- 2026-03-27T14:05:11Z [TOOL] The right-side contact copy was fully local to `ContactSection.jsx`; removing it required no content-data changes and left the method cards/layout intact, with lint/build still passing.
- 2026-03-27T14:03:50Z [TOOL] The contact header block was isolated from the form and info layout; collapsing it to a title-only header required only local content/component edits, and both the homepage section and standalone `/contact` build path remained valid with lint/build passing.
- 2026-03-27T14:02:10Z [TOOL] The blog preview header was structured like the old Works header; collapsing it to a title-only layout required only local component/content edits, and the posts grid plus CTA remained unaffected with lint/build still passing.
- 2026-03-27T14:00:45Z [TOOL] Reintroducing a title-only header for Works is structurally independent from the featured/archive panels; the shared heading styles slot back in cleanly without affecting project queries or card layout, and lint/build still pass.
- 2026-03-27T13:57:00Z [TOOL] The Works intro was self-contained inside `WorksSection.jsx`; removing the header block did not affect project loading, featured/archive grids, or the `#works` anchor, and no follow-on styling changes were required for lint/build to pass.
- 2026-03-27T13:46:22Z [TOOL] Source-level verification is clean, but a Playwright check against `http://localhost:5173/#resume` hit Vite client WebSocket `ECONNREFUSED` errors and still rendered stale pre-change button/title styles; treat that localhost browser result as environment-stale rather than evidence the new source edits failed.
- 2026-03-27T11:43:39Z [TOOL] Live localhost inspection with Playwright after the pass reported `resumeWidth: 1120`, `headerBackground: rgb(16, 16, 16)`, and `statBackground: rgba(0, 0, 0, 0)`, which confirms the second section is wider, the header is opaque, the 3D + VFX row no longer has its extra fill, and the first paragraph under `#resume` is now body copy rather than an eyebrow label.
- 2026-03-27T11:36:17Z [TOOL] The left hero rail does not depend on the removed status line for spacing or layout; deleting that entry is sufficient, with no additional styling changes needed for lint/build to pass.
- 2026-03-27T10:09:16Z [TOOL] A desktop Playwright scroll check against `http://localhost:5173/` showed the right follow rail keeping the same viewport top position before and after `window.scrollTo(...)`, and the post-scroll screenshot confirmed the left contact/status rail also remained visible over the scrolled content.
- 2026-03-27T09:46:00Z [TOOL] The remaining visible green on the homepage was coming from two places: shared accent tokens in `frontend/src/styles/theme.js` and a hardcoded radial glow color in `HomeHero.jsx`; changing both was enough to shift the visible theme blue across the hero and navigation.
- 2026-03-27T09:44:00Z [TOOL] The portfolio intros that read best for this profile type were consistently narrower than the original draft: they led with one role (`motion designer`, `designer/developer`) and then named a focused outcome area, rather than enumerating many adjacent disciplines in one sentence.
- 2026-03-27T09:38:00Z [TOOL] The broader availability sentence initially chosen was too long for the left hero rail at desktop width; shortening it prevented overlap with the intro block while preserving the user's intent.
- 2026-03-27T09:30:00Z [TOOL] The earlier embed implementation had two real path/runtime defects: `frontend/public/particle-earth/index.html` referenced `/assets/...` at the app root instead of relative bundle paths, and `ParticleGlobe.tsx` loaded `/earth-elevation.png` from the root instead of the embed base URL; together these prevented the hero globe from mounting correctly.
- 2026-03-27T09:30:00Z [TOOL] `useControls` from Leva still injects a root element even without rendering `<Leva />`; embed mode needs explicit CSS suppression of `#leva__root` or hidden debug UI will still appear in the iframe DOM.
- 2026-03-27T09:30:00Z [TOOL] Playwright verification after the fixes confirmed `hasDesktopNav: true`, `hasMenuButtonVisible: false`, `iframeHasCanvas: true`, `levaVisible: false`, and `heroRect.bottom > viewport.height`, which means the first-screen hero no longer exposes the next section at load.
- 2026-03-27T09:02:55Z [TOOL] The most obvious mismatch versus the reference was structural, not color-only: the previous pass had an oversized centered hero, oversized rounded panels, and a pill nav, while the reference relies on a sparse fixed header, tighter 920px-ish content width, and flatter section boxes.
- 2026-03-27T09:02:55Z [TOOL] The local `ParticleEarth` embed reads as a washed-out layer unless the iframe is deliberately darkened; applying iframe brightness/contrast filtering and a stronger dark hero shade makes it behave more like atmospheric background motion.
- 2026-03-27T08:10:03Z [TOOL] `frontend/scripts/designAudit.mjs` is blocked in this sandbox because Playwright launch fails with `browserType.launch: spawn EPERM`; local browser verification could not be completed through the repo audit script.
- 2026-03-27T08:10:03Z [TOOL] A temporary Python static server can be started and reached from PowerShell within a single shell session, but MCP Playwright browser tools cannot reach that in-process localhost server from a separate tool call in this environment.
- 2026-03-27T08:10:03Z [TOOL] `powershell -NoProfile -ExecutionPolicy Bypass -File frontend/scripts/syncParticleEarth.ps1` rebuilds and copies the earth bundle correctly, but the environment appends a Git Bash `couldn't create signal pipe` crash after success; rely on the exit code plus the sync log line rather than the trailing shell noise.
- 2026-02-09T10:55:33Z [TOOL] `npm install` cannot fetch uncached packages in this sandbox (`ENOTCACHED` / only-if-cached); avoid introducing new dependencies unless they are already present.

[OUTCOMES]
- 2026-03-28T00:34:00+08:00 [TOOL] The hero now ships with the requested hidden defaults (`#0d3b8b`, `64%`), Leva is hidden again in the embed, and the shared header is 20px shorter than the prior layout. Verification succeeded with direct ESLint execution in `ParticleEarth` and `frontend`, production builds for both apps, the canonical embed sync, and Playwright checks on `http://localhost:5173/`.
- 2026-03-28T00:24:00+08:00 [TOOL] The hero background now uses a radial gradient centered behind the globe while keeping the existing Leva color/length controls intact. Verification succeeded with direct ESLint execution in `ParticleEarth` and `frontend`, a rebuilt/synced `ParticleEarth` embed, and Playwright confirmation of the live iframe's computed background image on `http://localhost:5173/`.
- 2026-03-28T00:13:06.9870180+08:00 [TOOL] The earth hero no longer emits the Three.js `Clock` deprecation warning, and the Leva panel is readable again. Verification succeeded with direct ESLint execution in both `ParticleEarth` and `frontend`, production Vite builds for both apps, the canonical `frontend/scripts/syncParticleEarth.ps1` sync, and Playwright checks on `http://localhost:5173/` confirming zero console warnings plus `iframeFilter: none`.
- 2026-03-27T15:59:29.2068739Z [TOOL] The hero `ParticleEarth` embed now shows Leva controls again and exposes live `heroGradientColor` / `heroGradientLength` controls for the background gradient behind the globe. Verification succeeded with `ParticleEarth` direct ESLint execution, two production rebuilds (before and after the final embed-positioning fix), the canonical sync script, and Playwright checks confirming the embed panel is visible at `top: 108px`, `right: 16px` on `http://localhost:5173/` and that the new gradient controls are present in the iframe DOM.
- 2026-03-27T15:43:37.1886598Z [TOOL] The mobile-friendly frontend pass is now implemented: shared responsive tokens/breakpoints, safer drawer behavior, a simplified mobile hero, header-aware standalone routes, touch-friendly work filters/contact form, and a reduced-interactive `ParticleEarth` phone embed all ship together. Verification succeeded with direct `eslint` execution in both `frontend` and `ParticleEarth`, escalated Vite production builds for both apps, the canonical `frontend/scripts/syncParticleEarth.ps1` sync, and Playwright checks across `320x568`, `375x667`, `390x844`, `768x1024`, and `1024x1366` plus `/contact`, `/rnd`, `/project/sit-open-house-2026`, and `/rnd/shophouse-generator`.
- 2026-03-27T15:20:00Z [TOOL] The site now uses `#90D5FF` as its shared accent, including derived surfaces and the hero glow. Verification succeeded with direct `eslint` execution, an escalated Vite production build, and a source sweep confirming the previous `#8CB6DE` palette values were removed from `frontend/src`.
- 2026-03-27T15:15:00Z [TOOL] The About section no longer shows the three-card workflow strip and now flows directly into the capabilities cards. Verification succeeded with direct `eslint` execution, an escalated Vite production build, and a source sweep confirming the workflow markup/text was removed from `ResumeSection.jsx`.
- 2026-03-27T15:12:00Z [TOOL] The site now uses `#8CB6DE` as its shared accent, including theme-derived surfaces and the hero glow. Verification succeeded with direct `eslint` execution, an escalated Vite production build, and a source sweep confirming the old accent values were removed from `frontend/src`.
- 2026-03-27T15:08:00Z [TOOL] The desktop header now aligns its last nav item with the right-side `Follow Me` rail on wide screens. Verification succeeded with direct `eslint` execution, an escalated Vite production build, and a Playwright geometry check on `http://localhost:5173/` at `1920x1080`.
- 2026-03-27T14:59:52.5301322Z [TOOL] The shared header no longer shows `Download CV` on desktop or in the drawer. Verification succeeded with direct `eslint` execution and an escalated Vite production build.
- 2026-03-27T14:58:38.3775074Z [TOOL] The `/rnd` page title is now visible below the fixed header instead of being covered by it. Verification succeeded with direct `eslint` execution, an escalated Vite production build, and Playwright geometry checks before and after the fix.
- 2026-03-27T14:53:57.3665745Z [TOOL] The `/rnd` page now opens with a simple `Blog` header and the post grid directly below it, without the old top text box. Verification succeeded with direct `eslint` execution, an escalated Vite production build, and a Playwright localhost check on `/rnd`.
- 2026-03-27T14:50:39.1459363Z [TOOL] The homepage section titles now align with the text inside the panels instead of the outer box edge; verification succeeded with direct `eslint` execution, an escalated Vite production build, and a Playwright DOM measurement check on the live localhost page.
- 2026-03-27T14:36:55.0034935Z [TOOL] The homepage no longer includes a blog section; `Blog` in the fixed header/drawer now routes to the dedicated `/rnd` journal page and stays active there. Verification succeeded with direct `eslint` execution, an escalated Vite production build, and a Playwright localhost navigation check.
- 2026-03-27T14:31:15.9140421Z [TOOL] The homepage now updates the active nav item on scroll, fades the desktop side rails before the footer, safely renders contact methods without a `.map()` crash, uses the blue accent for the blog CTA, constrains sparse blog previews on wide screens, and uses a broader `1240px` desktop content width; verification succeeded with direct `eslint` execution, an escalated Vite production build, and a Playwright localhost desktop pass.
- 2026-03-27T14:16:44Z [TOOL] The codebase now removes the entire large footer content block and leaves only the minimal copyright footer; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:15:11Z [TOOL] The codebase now removes the footer implementation note and leaves only the copyright line in the footer bottom row; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:13:38Z [TOOL] The homepage now has larger gaps between sections and smaller section-title headers across About/Work/Blog/Contact; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:11:06Z [TOOL] The codebase now renders the contact-method cards horizontally across the section on desktop, with responsive collapse on smaller screens; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:09:32Z [TOOL] The codebase now places the contact methods panel below the message form instead of beside it; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:06:49Z [TOOL] The codebase now removes the separate featured-work block and places featured projects at the front of the main works grid, with the `Project Archive` label removed; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:05:11Z [TOOL] The codebase now removes the right-side contact intro copy and leaves only the direct contact-method cards in that panel; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:03:50Z [TOOL] The codebase now gives the contact section a single `Contact` heading and removes the old boxed intro copy, matching the other simplified section headers; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:02:10Z [TOOL] The codebase now gives the blog preview section a single `Blog` heading with the old intro copy removed, while keeping the `View All Posts` action; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T14:00:45Z [TOOL] The codebase now gives the Works section a single `Work` heading above `Featured Work`, matching the simpler About-section pattern; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T13:57:00Z [TOOL] The codebase now removes the top-level Works intro copy and starts the section directly at `Featured Work`; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T13:46:22Z [TOOL] The codebase now sets the second-section heading to `About` and applies the blue accent treatment to the resume action buttons and capability tool chips; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`, while live localhost browser confirmation remains UNCONFIRMED because the Playwright session hit stale Vite client socket errors.
- 2026-03-27T11:43:39Z [TOOL] The current localhost post-hero layout is broader and cleaner: no `Resume` eyebrow above the second section title, wider section headings, a solid fixed header, and a neutral 3D + VFX stats panel; verification succeeded with `frontend` `npm run lint`, `frontend` `npm run build`, and a Playwright localhost DOM check on `http://localhost:5173/#resume`.
- 2026-03-27T11:36:17Z [TOOL] The current localhost hero left rail now shows only the email line, with the status line removed; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T10:09:16Z [TOOL] The current localhost desktop homepage now keeps the bottom-left contact/status rail and right-side follow rail visible during scroll; verification succeeded with `frontend` `npm run lint`, `frontend` `npm run build`, and a Playwright scroll check plus screenshot on `http://localhost:5173/`.
- 2026-03-27T09:52:00Z [TOOL] The desktop hero now uses the requested exact bottom-line wording; verification succeeded with `frontend` `npm run lint` and `frontend` `npm run build`.
- 2026-03-27T09:46:00Z [TOOL] The current localhost hero and accent system now use the requested blue palette centered on `#4988C4`; verification succeeded with `frontend` `npm run lint`, `frontend` `npm run build`, and a Playwright screenshot showing the blue title accent and active nav underline on `http://localhost:5173/`.
- 2026-03-27T09:44:00Z [TOOL] The current localhost hero keeps the contact/status copy on the left rail, centers the `Hello, I'm...` block, and uses the simpler introduction without overlap; verification succeeded with `frontend` `npm run lint`, `frontend` `npm run build`, a Playwright desktop screenshot, and a final localhost check showing the revised line in the centered bottom position.
- 2026-03-27T09:38:00Z [TOOL] The current localhost hero now drops the brand dot, places the down-arrow in the right-side open space, and uses the shorter status line without overlapping the intro copy; verification succeeded with `frontend` `npm run lint`, `frontend` `npm run build`, and a final Playwright desktop check on `http://localhost:5173/`.
- 2026-03-27T09:30:00Z [TOOL] The current localhost desktop hero is materially closer to the reference: top-left brand cluster, top-right horizontal nav, full-screen first-stage composition, bottom-left contact block, right-side follow rail, centered statement, and an active interactive earth canvas behind the text.
- 2026-03-27T09:30:00Z [TOOL] Verification for this pass succeeded with `frontend` `npm run lint`, `frontend` `npm run build`, `ParticleEarth` `npm run lint`, `ParticleEarth` `npm run build`, `ParticleEarth` `npm run test:unit`, the canonical earth sync command, and Playwright checks on `http://localhost:5173/`; the only remaining runtime warning observed is the upstream Three.js `Clock` deprecation inside the earth bundle.
- 2026-03-27T09:02:55Z [TOOL] The localhost build is materially closer to the CVIO reference after a browser-guided second pass: fixed minimal header, hamburger drawer, tighter gutters, reduced heading sizes, darker hero, and flatter home-section framing.
- 2026-03-27T08:10:03Z [TOOL] The requested frontend revamp is implemented: dark-only CVIO-inspired shell, anchored one-page home, retained Sanity-backed portfolio/blog surfaces, reusable contact section, and harmonized secondary routes.
- 2026-03-27T08:10:03Z [TOOL] `ParticleEarth` now ships as the first-screen interactive hero through a dedicated embed build copied into `frontend/public/particle-earth`; the repo documentation now describes the sync workflow and active route structure.
- 2026-03-27T08:10:03Z [TOOL] Live browser screenshot/design-audit verification against a local server remains UNCONFIRMED in this sandbox because of Playwright/process restrictions (`spawn EPERM` and localhost isolation across tool boundaries).
