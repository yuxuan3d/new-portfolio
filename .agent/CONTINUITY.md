[PLANS]
- 2026-03-28T00:26:05.6192387+08:00 [CODE] [MILESTONE] Keep this file milestone-based and bounded; only append material deltas affecting the frontend shell, ParticleEarth embed, verification workflow, or known caveats.
- 2026-03-28T00:26:05.6192387+08:00 [ASSUMPTION] No open implementation request is pending after the latest hero/header pass; future work should treat the state below as the current baseline unless superseded by the user.

[DECISIONS]
- 2026-03-27T08:10:03Z [CODE] `/` is the canonical one-page route; `/about` redirects to `/#resume`; retained standalone surfaces are `/project/:slug`, `/rnd`, `/rnd/:slug`, and `/contact`.
- 2026-03-27T08:10:03Z [CODE] `ParticleEarth` stays isolated as a built iframe served from `frontend/public/particle-earth`; do not merge its runtime into the main React app.
- 2026-03-27T08:10:03Z [CODE] The canonical earth sync workflow is `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\syncParticleEarth.ps1`; prefer this over npm wrappers in this environment.
- 2026-03-27T15:43:37.1886598Z [CODE] Shared responsive breakpoints are `phone <= 640`, `tablet <= 1023`, `desktop >= 1024`, and `wide rails >= 1180`, with layout tokens centralized around `--site-gutter`, `--section-gap`, and `--panel-padding`.
- 2026-03-27T14:36:55.0034935Z [CODE] The homepage no longer previews blog content; the `Blog` nav item routes to the dedicated `/rnd` journal page instead of anchoring into `/`.
- 2026-03-27T15:20:00Z [USER] The shared accent color is `#90D5FF`, superseding earlier blue variants; derived buttons, chips, glows, and focus states follow this token.
- 2026-03-28T00:13:06.9870180+08:00 [CODE] The hero iframe should not use whole-frame dimming; readability comes from the page overlay plus in-iframe gradient/background treatment.
- 2026-03-28T00:13:06.9870180+08:00 [CODE] The removed Three.js `Clock` warning currently depends on a local patch inside `ParticleEarth/node_modules/@react-three/fiber/dist/*`; this is fragile and will be lost on reinstall until replaced by a durable patch workflow.
- 2026-03-28T00:34:00+08:00 [USER] Keep the hero gradient controls in code but hide Leva in embed mode for now; the shipped defaults are `#0d3b8b` and `64%`.
- 2026-03-28T00:34:00+08:00 [CODE] The shared header baseline is now `102px` desktop and `76px` tablet/mobile through `--site-header-height`; live measurement may read `+1px` because of the header border.

[PROGRESS]
- 2026-03-31T23:06:04.7535581+08:00 [CODE] Fixed the two review findings in `frontend/src/components/home/WorksSection.jsx`: the equality guard now treats refreshed project objects as changes instead of comparing `_id` order alone, and the filter effect now clears any pending timeout plus exit/enter state before returning on a no-op filter change.
- 2026-03-31T18:22:31.8922931+08:00 [CODE] Added animated Work filtering in `frontend/src/components/home/WorksSection.jsx`: nonmatching cards now fade out over `200ms`, the filtered list is staged through `displayedItems`, and surviving cards use a FLIP-style transform pass so they slide into their new grid positions instead of snapping.
- 2026-03-31T18:15:25.6853315+08:00 [CODE] Increased the ParticleEarth idle auto-rotation speed slightly by raising `INTERACTION_CONFIG.idleAutoRotateSpeed` from `0.03` to `0.04`, then rebuilt and resynced the iframe bundle into `frontend/public/particle-earth`.
- 2026-03-31T18:13:24.6779826+08:00 [CODE] Removed the homepage hero headline by clearing `HERO_CONTENT.displayLead/displayAccent` and making `HomeHero` render the large display title only when those fields are populated.
- 2026-03-31T18:11:48.7590323+08:00 [CODE] Removed the extra outer wrapper chrome around the three contact-method cards by stripping the `InfoPanel` border/background/padding, leaving only the individual method cards visible.
- 2026-03-31T18:10:39.6775847+08:00 [CODE] Removed the footer-based fade logic from the homepage email and `Follow Me` rails so both side rails remain fixed and fully visible at the bottom of the page.
- 2026-03-31T17:55:40.6047657+08:00 [CODE] Tightened the `The Arsenal` tool pills in project details by converting them from block-like text boxes into centered inline-flex chips with a fixed 34px height and no extra vertical padding.
- 2026-03-31T17:52:20.5908269+08:00 [CODE] Removed the extra rounded modal shell around overlay project details and made the full-screen dark backdrop the scrolling surface, with the close button pinned to the viewport instead of the inner container.
- 2026-03-31T17:49:04.4026533+08:00 [CODE] Reworked portfolio-item navigation into a route-backed modal pattern: homepage work cards now pass the current location as background state, `App` keeps the homepage rendered underneath `/project/:slug`, and `ProjectDetail` can render as either a standalone page or a closable overlay dialog.
- 2026-03-31T17:42:26.8965986+08:00 [CODE] Removed the header's old centered max-width cap and extra desktop nav right inset so the homepage header now sits directly on the hero rails instead of an inner centered container.
- 2026-03-29T00:24:00+08:00 [CODE] Promoted the shared header's `Contact` nav item into a dedicated CTA treatment on both desktop and the mobile drawer, using the accent fill, rounded button chrome, and the existing nav wiring instead of adding a separate header control.
- 2026-03-28T12:53:00+08:00 [CODE] Restored equal-height desktop project hero cards while shrinking the wide-screen image further: the hero row stretches both panels to the same height again, but the image itself is now capped at `340px` so the whole row is tighter.
- 2026-03-28T12:47:00+08:00 [CODE] Tightened the wide-desktop project hero further by stopping the left panel from stretching, reducing the wide-screen title clamp, and capping the main image at `420px` height so the side-by-side hero no longer consumes excessive vertical space at `1920x1080`.
- 2026-03-28T12:41:00+08:00 [CODE] Reduced the desktop project hero title scale and tightened line-height so the left title panel no longer dominates the side-by-side project hero next to the main image.
- 2026-03-28T12:36:00+08:00 [CODE] Removed the `Embedded Media` label from the project-detail media panel while keeping the video stack and panel chrome intact.
- 2026-03-28T12:31:00+08:00 [CODE] Reworked `ProjectDetail` so the project hero becomes a two-column row on desktop, with the title/tags block on the left and the main image on the right, while retaining the stacked single-column order on tablet/mobile.
- 2026-03-28T12:19:00+08:00 [CODE] Replaced the header's inner `100vw` width math with a bounded `min(var(--site-max-width), 100%)`, increased the Work card caption inset, and hardened `ContactSection` for narrow standalone widths by moving standalone gutters into section padding plus `min-width: 0` / `overflow-wrap` guards.
- 2026-03-28T11:31:45+08:00 [CODE] Removed the footer's separate dot/gradient layer and instead faded the homepage ContentBand dots out near the bottom, so the footer now sits on the same `surfaceAlt` base without a restarted black band or misaligned secondary grid.
- 2026-03-28T11:12:03.5949627+08:00 [CODE] Tightened the About-band dot mask so it reaches full visibility sooner, bringing the dots up closer to the `About` heading instead of delaying their appearance until the lower stat/capability rows.
- 2026-03-28T11:08:53.6952811+08:00 [CODE] Split the About/content-band dots into a masked pseudo-element layer so the dot field fades in from the top of the section instead of appearing as an abrupt hard edge.
- 2026-03-28T11:04:57.9561963+08:00 [CODE] Moved the hero dot field from the broader `app-shell` background to `scene-frame::before` inside `ParticleEarth`, placing it on a dedicated layer behind the canvas so the dots remain visible in the hero background without crossing in front of the globe.
- 2026-03-28T10:52:38.3774831+08:00 [CODE] Removed the gradient image overlay from the homepage Work thumbnails and switched the project titles to a text-shadow treatment for readability instead of tinting the images.
- 2026-03-28T10:49:42.4651451+08:00 [CODE] Moved the hero dot pattern out of the frontend `HeroShade` overlay and into `ParticleEarth`'s own `app-shell` background so the dots render behind the globe; reduced dot opacity from `0.20` to `0.15` across the home/content/global treatments.
- 2026-03-28T10:20:47.2116558+08:00 [CODE] Moved the dotted pattern into the actual home-page hero shade and post-hero content-band backgrounds after screenshots showed the global overlay was still hidden by those section backgrounds.
- 2026-03-28T10:13:13.6022830+08:00 [CODE] Replaced the old global line-grid overlay with a repeated white radial-dot background pattern so the site now carries a subtle dotted grid across the shell at about 20% opacity.
- 2026-03-28T10:04:45.5227281+08:00 [CODE] Removed the inner header row's top padding and restored true flex centering so the brand and desktop nav sit on the header centerline instead of riding low inside the shortened header.
- 2026-03-28T09:53:09.1939431+08:00 [CODE] Wrapped the post-hero home sections in a shared content band with a vertical gradient so the transition from the black hero into the lower grey section background is continuous instead of abrupt.
- 2026-03-28T09:21:12.0992248+08:00 [CODE] Removed the outer framed wrapper from the homepage Work section so the filters and project grid sit directly in the page flow while keeping their existing internal spacing.
- 2026-03-27T08:10:03Z [TOOL] [MILESTONE] The frontend was rebuilt into a dark-only, CVIO-inspired shell using existing Sanity portfolio/blog data and EmailJS contact handling, with secondary routes restyled to match the main site.
- 2026-03-27T09:02:55Z [TOOL] [MILESTONE] Playwright-guided desktop passes moved the live localhost site materially closer to the CVIO reference: fixed desktop nav, hero framing with side rails, tighter content rhythm, simplified panels, and reference-style spacing/alignment.
- 2026-03-27T14:00:00Z [TOOL] [MILESTONE] The homepage was simplified into title-led sections: About/Work/Contact use stripped headers, the footer was reduced to a minimal copyright row, featured work was folded into the main works grid, and the dedicated `/rnd` page was reduced to a title plus post grid.
- 2026-03-27T14:36:55.0034935Z [TOOL] [MILESTONE] The blog preview was removed from the homepage and the fixed/drawer `Blog` nav was rewired to the separate `/rnd` route, which remains part of the active site structure.
- 2026-03-27T15:43:37.1886598Z [TOOL] [MILESTONE] The mobile-friendly pass landed across `/`, `/contact`, `/rnd`, `/rnd/:slug`, and `/project/:slug`: shared breakpoints/tokens, drawer-safe nav, a capped mobile hero, header-aware standalone routes, touch-friendly works/contact UI, and a reduced-interactive earth embed on phones.
- 2026-03-28T00:13:06.9870180+08:00 [TOOL] [MILESTONE] The earth hero background system evolved through multiple passes: Leva controls were restored, gradient color/length became configurable, the old `Clock` warning was removed, the hero background became a globe-centered radial gradient, and the current shipped state hides Leva again while keeping the tuned defaults in code.

[DISCOVERIES]
- 2026-03-31T18:22:31.8922931+08:00 [TOOL] Playwright MCP verification is currently blocked in this session by `EPERM: operation not permitted, mkdir 'C:\\Windows\\System32\\.playwright-mcp'`; direct `frontend` ESLint and Vite production builds still work, so Work-filter verification for this pass remained code/build-level rather than live browser capture.
- 2026-03-28T12:05:00+08:00 [TOOL] A Playwright sweep across `/`, `/rnd`, `/rnd/:slug`, `/project/:slug`, and `/contact` at `320x568`, `390x844`, `768x1024`, and `1024x1366` found no page-level horizontal scrolling or media overflow on the live site. One residual quirk remains on `/contact` at `320px`: layout math reports `scrollWidth: 349` vs `innerWidth: 320`, but `overflow-x: hidden` prevents actual horizontal scroll and the page still renders correctly in screenshots.
- 2026-03-28T10:15:18.6185421+08:00 [TOOL] The dotted-grid CSS existed but was invisible because `body::before` and `body::after` were both painted at negative z-index behind the body background; the overlay only becomes visible when those pseudo-elements are raised to `z-index: 0` and the app root is explicitly layered above them.
- 2026-03-27T09:30:00Z [TOOL] The earth embed only works reliably when its assets resolve relative to the `/particle-earth/` base path; absolute root asset paths broke the hero iframe.
- 2026-03-27T09:30:00Z [TOOL] `useControls` can still inject a Leva root even when `<Leva />` is not rendered; embed mode needs explicit CSS suppression if the panel should stay hidden.
- 2026-03-27T08:10:03Z [TOOL] The sync script copies the earth build successfully, but this environment may append a Git Bash `couldn't create signal pipe` message after success; trust the successful sync log and exit code rather than the trailing shell noise.
- 2026-03-27T08:10:03Z [TOOL] `frontend/scripts/designAudit.mjs` remains blocked in this sandbox by `browserType.launch: spawn EPERM`; direct Playwright MCP checks against `http://localhost:5173/` work and became the reliable browser-verification path.
- 2026-03-27T15:43:37.1886598Z [TOOL] Playwright's `320x568` mobile run reported a `scrollWidth` mismatch once, but page-level horizontal scrolling was not actually possible because `overflow-x: hidden` kept `window.scrollX` at `0`.
- 2026-03-28T00:13:06.9870180+08:00 [TOOL] The `Clock` deprecation warning originated from `@react-three/fiber` internals rather than local scene code, which is why the current fix lives in patched library bundles instead of app components.
- 2026-03-27T08:10:03Z [TOOL] This sandbox cannot fetch uncached npm packages (`ENOTCACHED` / cache-only failures), so avoid adding new dependencies unless they are already present.

[OUTCOMES]
- 2026-03-31T23:06:04.7535581+08:00 [TOOL] The animated Work filter no longer races stale timeouts on quick filter reversions and no longer keeps stale project objects when Sanity refreshes preserve the same id/order. Verification succeeded with direct `frontend` ESLint and Vite production build checks after patching the equality guard and the timeout-clearing path.
- 2026-03-31T18:22:31.8922931+08:00 [TOOL] The homepage Work filter now animates instead of hard-swapping cards: excluded items fade out over `0.2s` and retained items reposition through a FLIP transform transition. Verification succeeded with direct `frontend` ESLint and Vite production build checks; live Playwright MCP confirmation was blocked in this session by the current `System32` temp-directory `EPERM`.
- 2026-03-31T18:15:25.6853315+08:00 [TOOL] The ParticleEarth hero now auto-rotates a bit faster at idle. Verification succeeded with direct `ParticleEarth` ESLint and the canonical `frontend/scripts/syncParticleEarth.ps1` workflow, which rebuilt the earth app and synced it into `frontend/public/particle-earth`.
- 2026-03-31T18:13:24.6779826+08:00 [TOOL] The homepage no longer shows the large `I build cinematic worlds.` hero headline. Verification succeeded with direct `frontend` ESLint and Vite production build checks after clearing the hero display copy and adding conditional rendering in `HomeHero`.
- 2026-03-31T18:11:48.7590323+08:00 [TOOL] The three contact method cards no longer sit inside an additional outer box. Verification succeeded with direct `frontend` ESLint and Vite production build checks after removing the `InfoPanel` panel chrome in `ContactSection`.
- 2026-03-31T18:10:39.6775847+08:00 [TOOL] The homepage side rails no longer fade out near the footer. Verification succeeded with direct `frontend` ESLint and Vite production build checks after removing the rail-opacity state/effect from `HomeHero`.
- 2026-03-31T17:55:40.6047657+08:00 [TOOL] The Arsenal pills are now vertically centered and shorter. Playwright on `http://localhost:5173/project/sit-open-house-2026` measured the first chip at `chipHeight: 34`, `chipDisplay: flex`, `chipAlignItems: center`, `chipJustifyContent: center`, and zero vertical padding after the fix.
- 2026-03-31T17:52:20.5908269+08:00 [TOOL] Overlay project details now sit directly on the dimmed page instead of inside an extra rounded base card. Playwright confirmed the overlay shell uses `backdropOverflowY: auto`, spans `left: 0` to `right: 1440`, and the dialog itself has `borderTopWidth: 0px`, `borderRadius: 0px`, `backgroundColor: rgba(0, 0, 0, 0)`, and `boxShadow: none`; the close button is `position: fixed` at roughly `19px` from the top-right viewport edge.
- 2026-03-31T17:49:04.4026533+08:00 [TOOL] Work cards on `/` now open project details as a dimmed overlay instead of replacing the page. Playwright on `http://localhost:5173/` confirmed `/project/sit-open-house-2026` opens with `hasDialog: true`, `hasCloseButton: true`, `bodyOverflow: hidden`, `backdropBackground: rgba(5, 6, 8, 0.68)`, and both backdrop click and the `X` return the route to `/`; mobile verification at `390x844` confirmed the same dialog pattern with `dialogWidth: 366`.
- 2026-03-31T17:42:26.8965986+08:00 [TOOL] The homepage header now aligns to the hero side rails on desktop. Playwright on `http://localhost:5173/` at `1920x1080` measured `brand.left: 36`, `emailLine.left: 36`, `contact.right: 1869`, and `follow.right: 1869` after the header width/inset change.
- 2026-03-29T00:24:00+08:00 [TOOL] The live header now emphasizes `Contact` as a filled button CTA. Direct `frontend` ESLint and Vite production builds passed, and Playwright on `http://localhost:5173/` measured the desktop `Contact` link with `background: rgb(144, 213, 255)`, `borderRadius: 999px`, and `minHeight: 42px`.
- 2026-03-28T12:53:00+08:00 [TOOL] The project-detail hero now uses equal-height title/image cards on wide desktop with a smaller image. Playwright on `/project/cinder` at `1920x1080` measured `heroHeight: 364`, `imagePanelHeight: 364`, and `imageHeight: 340`, with the updated capture at `output/playwright/review-project-1920-equal-height-smaller.png`.
- 2026-03-28T12:47:00+08:00 [TOOL] The project-detail hero is now materially smaller on wide desktop. Playwright on `/project/cinder` at `1920x1080` measured `titleFontSize: 52px`, `titleHeight: 48`, `heroHeight: 119`, and `imageHeight: 420`, with the updated capture at `output/playwright/review-project-1920-tightened.png`.
- 2026-03-28T12:41:00+08:00 [TOOL] Project detail titles are now smaller and shorter in the side-by-side desktop hero. Playwright on `/project/sit-open-house-2026` at `1383x768` measured the title at `66.384px` font size with `61.0733px` line height and captured the updated layout at `output/playwright/review-project-title-reduced.png`.
- 2026-03-28T12:36:00+08:00 [TOOL] Project detail pages no longer render the `Embedded Media` heading above the video panel. Verification succeeded with direct `frontend` ESLint/build checks and a Playwright check on `/project/cinder` confirming `hasEmbeddedMediaText: false`.
- 2026-03-28T12:31:00+08:00 [TOOL] Individual project pages now show the title block and main image side by side on desktop. Playwright verified `grid-template-columns: 563.031px 660.969px` on `/project/betadine-sore-throat-lozenges` at `1499x900`, with the desktop capture at `output/playwright/review-project-hero-side-by-side.png`; mobile still collapses to one column, verified at `390x844` in `output/playwright/review-project-hero-mobile-stack.png`.
- 2026-03-28T12:19:00+08:00 [TOOL] The residual `/contact` `320px` width mismatch is fixed. Playwright now reports `scrollWidth: 320`, `innerWidth: 320`, and no overflow offenders on `http://localhost:5173/contact`, with the updated live capture at `output/playwright/review-contact-320-fixed.png`. The desktop header still fits the shell (`headerInnerWidth: 1240` at `1440px`) and the Work overlay inset increased to `21.6px`, verified in `output/playwright/review-home-desktop-fixed.png`.
- 2026-03-28T12:05:00+08:00 [TOOL] Playwright review artifacts were captured for desktop and mobile (`review-home-desktop.png`, `review-home-mobile-full.png`, `review-home-mobile-menu.png`, `review-blog-mobile.png`, `review-blog-post-mobile.png`, `review-project-mobile.png`, `review-contact-mobile.png`, `review-contact-320.png`). Current verdict: mobile works across the tested live routes, with the remaining actionable issue being the `/contact` container width calculation on the narrowest `320px` viewport.
- 2026-03-28T11:31:45+08:00 [TOOL] The footer seam is now visually continuous with the section above: the black strip is gone, the footer no longer restarts its own dot field, and Playwright confirmed the live footer has `background-image: none` while the ContentBand dot mask fades out before the footer. Verification succeeded with direct `frontend` ESLint/build checks and a live screenshot at `output/playwright/footer-seam-fixed.png`.
- 2026-03-28T11:12:03.5949627+08:00 [TOOL] The About-section dots now fade in earlier and less aggressively. Verification succeeded with direct `frontend` ESLint/build checks and a Playwright screenshot at `output/playwright/dots-about-fade-earlier.png`.
- 2026-03-28T11:08:53.6952811+08:00 [TOOL] The About-section dot pattern now fades in from the top rather than starting abruptly at the hero/content boundary. Verification succeeded with direct `frontend` ESLint/build checks and a Playwright screenshot at `output/playwright/dots-about-fade.png`.
- 2026-03-28T11:04:57.9561963+08:00 [TOOL] The hero dot field is now visible in the hero background while staying behind the particle earth globe. Verification succeeded with direct `ParticleEarth` ESLint/build checks, the canonical earth sync script, and a fresh Playwright screenshot at `output/playwright/dots-behind-earth-v2.png`.
- 2026-03-28T10:52:38.3774831+08:00 [TOOL] The Work thumbnails now show their images without the old dark gradient overlay, while the title labels stay readable through a drop-shadow text treatment. Verification succeeded with direct ESLint and Vite production build checks in `frontend`.
- 2026-03-28T10:49:42.4651451+08:00 [TOOL] The hero dots now render behind the particle earth instead of over it, and the dot field is lighter. Verification succeeded with direct ESLint/Vite builds for `frontend` and `ParticleEarth`, the canonical earth sync script, and a Playwright hero screenshot at `output/playwright/dots-behind-earth.png`.
- 2026-03-28T10:20:47.2116558+08:00 [TOOL] Playwright screenshots now visibly show the dotted pattern on the home page: the hero shot at `output/playwright/dots-top-after.png` and the about/work shot at `output/playwright/dots-about-after.png` both render the white dot grid across the visible background areas.
- 2026-03-28T10:15:18.6185421+08:00 [TOOL] The dotted background issue is fixed at the stacking level. Playwright on `http://localhost:5173/` now reports `body::before z-index: 0`, `opacity: 0.2`, the expected radial-dot background image, and `#root` at `position: relative; z-index: 1`, so the overlay is in the visible background layer instead of behind the page.
- 2026-03-28T10:13:13.6022830+08:00 [TOOL] The site background now uses a dotted grid overlay instead of vertical guide lines. Playwright confirmed `body::before` is `radial-gradient(circle, rgb(255, 255, 255) 0.85px, transparent 1.05px)` with `background-size: 28px 28px` and `opacity: 0.2` on the live localhost page.
- 2026-03-28T10:04:45.5227281+08:00 [TOOL] The shared header content is vertically centered again. Playwright on `http://localhost:5173/` measured the brand and desktop nav at about `-0.5px` from the header center after the fix, versus roughly `+5px` low before it.
- 2026-03-28T09:53:09.1939431+08:00 [TOOL] The homepage now fades from the black hero into the lower grey content area through a post-hero vertical gradient band; Playwright confirmed the live wrapper behind `About` uses `linear-gradient(rgb(5, 6, 8) 0%, rgb(10, 11, 13) 12%, rgb(20, 20, 20) 100%)`.
- 2026-03-28T09:21:12.0992248+08:00 [TOOL] The homepage Work section no longer has the outer box outline; the internal filter rail and cards remain unchanged apart from losing that enclosing border/background chrome.
- 2026-03-28T00:34:00+08:00 [TOOL] Current verified hero/header state: hero background defaults are `#0d3b8b` and `64%`, Leva is hidden again in embed mode, the earth hero uses a globe-centered radial gradient, whole-iframe darkening is removed, and the shared header is 20px shorter than the prior layout.
- 2026-03-28T00:34:00+08:00 [TOOL] Latest verification succeeded with direct ESLint execution in `frontend` and `ParticleEarth`, direct Vite production builds for both apps, the canonical earth sync command, and Playwright checks on `http://localhost:5173/`.
- 2026-03-27T15:43:37.1886598Z [TOOL] The responsive/mobile pass is verified across `320x568`, `375x667`, `390x844`, `768x1024`, and `1024x1366`, plus `/contact`, `/rnd`, `/project/sit-open-house-2026`, and `/rnd/shophouse-generator`.
- 2026-03-27T14:36:55.0034935Z [TOOL] The main site now flows from Home to Work to Contact on `/`, while `Blog` lives on `/rnd`; this route split was verified with Playwright against the live localhost build.
- 2026-03-27T08:10:03Z [TOOL] The repo now operates as a dark, one-page portfolio shell with a synced `ParticleEarth` hero and retained Sanity-backed project/blog detail routes; this is the active product baseline for future changes.
